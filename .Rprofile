# fancy fonts for our workshop plots (only when saving them)
systemfonts::register_font(
  "sans",
  plain = "/usr/local/share/fonts/Inter-Regular.ttf",
  bold = "/usr/local/share/fonts/Inter-Bold.ttf")
